# Fauna.by: Интернет-магазин зоотоваров

## О проекте

Fauna.by — это полнофункциональный интернет-магазин товаров для животных, разработанный с использованием React, TypeScript и TailwindCSS. Проект представляет собой клиентскую часть (фронтенд) с эмуляцией бэкенд-функциональности через локальное хранилище данных.

### Основные возможности

- 🛍️ **Просмотр каталога товаров** с фильтрацией и сортировкой
- 📋 **Детальные страницы товаров** с галереей, вариантами, ценами и отзывами
- 🛒 **Корзина** с возможностью изменения количества товаров
- 📦 **Оформление заказов** и сохранение их в истории
- 📜 **История заказов** с возможностью повторного заказа
- 🌙 **Темная/светлая тема** интерфейса
- 👤 **Авторизация и личный кабинет** пользователя
- 📱 **Адаптивный дизайн** для всех типов устройств

## Содержание документации

1. [Технические требования](#технические-требования)
2. [Установка и запуск](#установка-и-запуск)
3. [Структура проекта](#структура-проекта)
4. [Архитектура приложения](#архитектура-приложения)
5. [Основные компоненты](#основные-компоненты)
6. [Маршрутизация](#маршрутизация)
7. [Управление состоянием](#управление-состоянием)
8. [Пользовательские сценарии](#пользовательские-сценарии)
9. [Разработка и сборка](#разработка-и-сборка)
10. [Журнал разработки](#журнал-разработки)

## Технические требования

Для успешной работы с проектом необходимы следующие компоненты:

- **Node.js** (версия 18.0.0 или выше)
- **npm** (версия 9.0.0 или выше) или **yarn** (версия 1.22.0 или выше)
- Современный браузер (рекомендуется Chrome, Firefox, Edge последних версий)

### Используемые технологии

- **React** (18.2.0) - библиотека для создания пользовательских интерфейсов
- **TypeScript** (4.9.5) - типизированный JavaScript для более надежного кода
- **React Router** (7.0.0) - библиотека для маршрутизации
- **TailwindCSS** (3.3.0) - утилитарный CSS-фреймворк
- **Vite** (4.4.0) - быстрый инструмент сборки
- **LocalStorage API** - для хранения данных на стороне клиента

## Установка и запуск

### Клонирование репозитория

```bash
git clone https://github.com/your-username/fauna-by.git
cd fauna-by
```

### Установка зависимостей

```bash
# С использованием npm
npm install

# Или с использованием yarn
yarn install
```

### Запуск в режиме разработки

```bash
# С использованием npm
npm run dev

# Или с использованием yarn
yarn dev
```

После успешного запуска приложение будет доступно по адресу [http://localhost:5173](http://localhost:5173).

### Сборка для продакшена

```bash
# С использованием npm
npm run build

# Или с использованием yarn
yarn build
```

Скомпилированные файлы будут доступны в директории `dist/`. Эти файлы можно разместить на любом статическом хостинге.

## Структура проекта

```
kurs3/
├── public/                  # Публичные статические файлы
├── src/                     # Исходный код
│   ├── assets/              # Изображения, шрифты и прочие ресурсы
│   ├── components/          # Многоразовые компоненты
│   │   ├── CartItem/        # Компонент элемента корзины
│   │   ├── HistoryProductCard/ # Компонент карточки товара в истории
│   │   ├── Modal/           # Компонент модального окна
│   │   ├── ProductCard/     # Компонент карточки товара в каталоге
│   │   └── ...              # Другие компоненты
│   ├── context/             # React контексты для глобального состояния
│   │   ├── AuthContext.tsx  # Авторизация пользователя
│   │   ├── CartContext.tsx  # Корзина
│   │   ├── HistoryContext.tsx # История заказов
│   │   └── ThemeContext.tsx # Темная/светлая тема
│   ├── data/                # Данные (моковые)
│   │   └── cards.json       # Данные о товарах
│   ├── layouts/             # Шаблоны для страниц
│   │   └── Main.tsx         # Основной шаблон с навигацией
│   ├── pages/               # Страницы сайта
│   │   ├── 404/             # Страница 404
│   │   ├── AboutUs/         # О нас
│   │   ├── Account/         # Личный кабинет
│   │   ├── Cart/            # Корзина
│   │   ├── Catalog/         # Каталог товаров
│   │   ├── History/         # История заказов
│   │   ├── Home/            # Главная страница
│   │   ├── Login/           # Авторизация
│   │   ├── Product/         # Детальная страница товара
│   │   └── Register/        # Регистрация
│   ├── App.tsx              # Главный компонент приложения
│   ├── index.tsx            # Точка входа
└── package.json           # Зависимости и скрипты
```

## Архитектура приложения

Приложение Fauna.by построено по модульной архитектуре с использованием компонентного подхода React и Context API для управления состоянием.

### Принципы архитектуры:

1. **Модульность** — каждый компонент имеет свою ответственность и может быть переиспользован
2. **Разделение ответственности** — бизнес-логика отделена от представления
3. **Однонаправленный поток данных** — данные передаются от родительских компонентов к дочерним
4. **Глобальное состояние** — через Context API для доступа к общим данным

### Ключевые элементы архитектуры:

- **Компоненты** — переиспользуемые блоки UI (ProductCard, CartItem и т.д.)
- **Контексты** — глобальное состояние приложения (корзина, авторизация и т.д.)
- **Страницы** — компоненты верхнего уровня, отображаемые при переходе по маршрутам
- **Layouts** — шаблоны страниц, содержащие общие элементы (шапка, подвал)
- **Роутер** — управление навигацией и маршрутизацией

## Маршрутизация

Для навигации используется React Router DOM v7 с определением следующих маршрутов:

```tsx
<Routes>
  <Route path="/" element={<Main />}>
    <Route index element={<Home />} />
    <Route path="/catalog" element={<Catalog />} />
    <Route path="/catalog/:category" element={<Catalog />} />
    <Route path="/about" element={<AboutUs />} />
    <Route path="/account" element={<Account />} />
    <Route path="/cart" element={<Cart />} />
    <Route path="/history" element={<History />} />
    <Route path="/login" element={<Login />} />
    <Route path="/register" element={<Register />} />
    <Route path="/product/:id" element={<Product />} />
    <Route path="/404" element={<Site404 />} />
    <Route path="*" element={<Site404 />} />
  </Route>
</Routes>
```

## Управление состоянием

Приложение использует Context API для глобального состояния:

### AuthContext

Управление авторизацией пользователя:

```tsx
// Использование
const { user, login, logout, register } = useAuth();

// Методы
login(email, password) // авторизация пользователя
logout() // выход из системы
register(userData) // регистрация нового пользователя
```

### CartContext

Управление корзиной покупок:

```tsx
// Использование
const { cartItems, addToCart, removeFromCart, updateQuantity, clearCart } = useCart();

// Методы
addToCart(item) // добавление товара в корзину
removeFromCart(id) // удаление товара из корзины
updateQuantity(id, quantity) // изменение количества
clearCart() // очистка корзины
```

### HistoryContext

Управление историей заказов:

```tsx
// Использование
const { orders, addOrder, removeOrder, repeatOrder } = useHistoryOrders();

// Методы
addOrder(order) // добавление заказа в историю
removeOrder(orderId) // удаление заказа из истории
repeatOrder(orderId) // повторное размещение заказа
```

### ThemeContext

Управление темой интерфейса:

```tsx
// Использование
const { darkMode, toggleTheme } = useTheme();

// Методы
toggleTheme() // переключение между светлой и темной темой
```

## Основные компоненты

### Компоненты интерфейса

- **Header** — верхняя панель с логотипом, навигацией и иконками
- **Footer** — нижняя часть сайта с информацией и ссылками
- **Modal** — модальное окно для форм и уведомлений
- **ProductCard** — карточка товара для отображения в каталоге
- **CartItem** — элемент корзины с управлением количеством
- **HistoryProductCard** — карточка товара в истории заказов
- **Slider** — слайдер для баннеров и карточек товаров

### Страницы

- **Home** — главная страница с баннерами и популярными товарами
- **Catalog** — каталог товаров с фильтрацией и сортировкой
- **Product** — детальная страница товара с информацией и отзывами
- **Cart** — корзина с товарами и оформлением заказа
- **History** — история заказов с возможностью повторного заказа
- **Account** — личный кабинет пользователя
- **Login/Register** — страницы авторизации и регистрации

## Пользовательские сценарии

### Сценарий 1: Просмотр каталога и добавление товара в корзину

1. Пользователь заходит на главную страницу (`/`)
2. Переходит в каталог товаров (`/catalog`)
3. Использует фильтры для поиска нужного товара
4. Находит интересующий товар и кликает на его карточку
5. На странице товара (`/product/:id`) просматривает описание, фото и отзывы
6. Выбирает нужный вариант (вес, размер и т.д.)
7. Указывает количество товара
8. Нажимает “В корзину”
9. Получает уведомление об успешном добавлении товара

### Сценарий 2: Оформление заказа

1. Пользователь переходит в корзину (`/cart`)
2. Проверяет список товаров и при необходимости изменяет количество
3. Нажимает “Оформить заказ”
4. Заполняет форму с данными доставки (если не авторизован)
5. Подтверждает заказ
6. Получает подтверждение об успешном оформлении
7. Заказ сохраняется в истории

### Сценарий 3: Просмотр истории заказов и повторный заказ

1. Пользователь авторизуется (если не сделал этого ранее)
2. Переходит в личный кабинет (`/account`)
3. Выбирает раздел “История заказов” или переходит напрямую по `/history`
4. Просматривает список своих заказов
5. Находит нужный заказ и нажимает “Повторить” или “Удалить”
6. При повторном заказе товары добавляются в корзину

### Сценарий 4: Оставление отзыва на товар

1. Пользователь находит товар через каталог или историю заказов
2. Переходит на страницу товара
3. Нажимает кнопку “Оставить отзыв”
4. В открывшемся модальном окне:
   - Вводит своё имя
   - Пишет текст отзыва
   - Выставляет оценку (от 1 до 5 звёзд)
   - Нажимает “Отправить”
5. Отзыв добавляется к товару и становится видимым для других пользователей

## Журнал разработки
- Инициализирован проект с использованием Vite, React и TypeScript
- Настроен Tailwind CSS для стилизации
- Создана базовая структура директорий
- Добавлены основные маршруты с использованием React Router v7

### Этап 2: Разработка компонентов Header и Footer (Дата: 14.05.2025)
- Создан компонент Header с логотипом, навигацией и поиском
- Реализована адаптивная верстка Header для разных экранов
- Добавлен компонент Footer с контактной информацией и социальными сетями
- Реализован переключатель темного/светлого режима

### Этап 3: Страница 404 и модальное окно (Дата: 14.05.2025)
- Разработан компонент страницы 404 с информативным сообщением и кнопками
- Создан универсальный компонент Modal для отображения модальных окон
- Реализована форма обращения в поддержку (SupportForm)
- Интегрировано модальное окно с формой поддержки на страницу 404

### Этап 4: Подготовка данных товаров (Дата: 15.05.2025)
- Создан JSON-файл с информацией о товарах (cards.json)
- Добавлены 30 товаров в разных категориях с подробным описанием
- Реализована структура данных для отзывов пользователей

### Этап 5: Разработка слайдеров (Дата: 15.05.2025)
- Разработан базовый компонент Slider с адаптивностью и анимацией
- Создан компонент баннерного слайдера (BannerSlider) для главной страницы
- Реализован компонент слайдера товаров (ProductSlider) для отображения карточек
- Создан модульный компонент карточки товара (ProductCard) для переиспользования
- Интегрированы все слайдеры на главную страницу

## Текущее состояние проекта

На данный момент реализованы:

1. **Компоненты пользовательского интерфейса**:
   - Header: навигация по категориям, поиск, переключение темы
   - Footer: контактная информация, социальные сети
   - Modal: универсальный компонент модального окна
   - SupportForm: форма обращения в поддержку
   - Страница 404: кастомная страница ошибки
   - Slider: базовый компонент слайдера с адаптивностью
   - BannerSlider: слайдер баннеров с автопрокруткой
   - ProductSlider: слайдер для отображения товаров
   - ProductCard: отдельный компонент карточки товара

2. **Функциональность**:
   - Поддержка светлой и тёмной темы с автоматической адаптацией компонентов
   - Открытие/закрытие модального окна с формой поддержки
   - Базовая валидация формы и имитация отправки запроса в поддержку
   - Динамическая загрузка данных товаров из JSON-файла
   - Автоматическая прокрутка баннеров на главной странице
   - Адаптивное отображение товаров в слайдерах в зависимости от размера экрана
   - Отображение рейтинга товаров на основе отзывов

3. **Технические особенности**:
   - Адаптивный дизайн для всех компонентов
   - Обработка закрытия модального окна по клику вне окна и по нажатию ESC
   - Блокировка скролла страницы при открытом модальном окне

## Планы на следующие этапы

1. **Этап 6: Полноценный каталог товаров**
   - Создание полной страницы каталога с отображением всех товаров
   - Реализация фильтрации и сортировки товаров
   - Создание детальной страницы товара с полным описанием

2. **Этап 7: Корзина и оформление заказа**
   - Добавление функциональности корзины
   - Разработка процесса оформления заказа
   - Интеграция различных способов оплаты

3. **Этап 6: Личный кабинет пользователя**
   - Создание страниц регистрации и входа
   - Разработка функционала личного кабинета
   - Добавление истории заказов

## Используемые технологии

- **Frontend**: React, TypeScript, Tailwind CSS
- **Сборка**: Vite
- **Маршрутизация**: React Router v7
- **Управление состоянием**: React Hooks (useState, useEffect, useRef)

## Инструкция по запуску проекта

```bash
# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev
```

## Структура проекта

```
src/
├── assets/             # Изображения и статические ресурсы
├── components/         # Переиспользуемые компоненты
│   ├── Header/         # Компонент заголовка сайта
│   ├── Footer/         # Компонент подвала сайта
│   ├── Modal/          # Универсальный компонент модального окна
│   ├── SupportForm/    # Форма обращения в поддержку
│   ├── Slider/         # Базовый компонент слайдера
│   ├── BannerSlider/   # Слайдер баннеров для главной страницы
│   ├── ProductSlider/  # Слайдер для отображения товаров
│   └── ProductCard/    # Компонент карточки товара
├── layouts/            # Макеты страниц
│   └── Main.tsx        # Главный макет с хедером и футером
├── pages/              # Компоненты страниц
│   └── 404/            # Страница 404
└── data/               # Файлы данных
    ├── users.json      # Данные пользователей
    ├── cart.json       # Данные корзины
    └── card.json       # Данные карточек товаров
```